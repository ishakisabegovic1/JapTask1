import {UserStudent} from "./UserStudent";

export interface Student{
    id: number,
    name: string,
    dateOfBirth: Date,
    address: string,
    status: string,
    selectionId: number,
   
}

